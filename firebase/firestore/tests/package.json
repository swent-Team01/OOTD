{
  "name": "ootd-firestore-rules-tests",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "engines": { "node": ">=18" },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^4.0.1",
    "firebase": "^11.0.0",
    "patch-package": "^8.0.0",
    "vitest": "^1.5.0"
  },
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest"
  }
}
